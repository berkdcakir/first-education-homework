[package]
name = "build"
edition = "2024.beta"

[dependencies]
capability = { local = "../capability" }

[addresses]
build = "0x0"
capability = "0x0"

[dev-dependencies]

[dev-addresses]
